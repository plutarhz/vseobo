import { Open_Sans } from "next/font/google";
import { Literata } from "next/font/google";

export const literata = Literata({ subsets: ['cyrillic'], display: 'swap', })
export const opensans = Open_Sans({ subsets: ['cyrillic'], display: 'swap', })